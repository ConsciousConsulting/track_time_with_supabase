/**
 * Admin reports — filter by project/user/date, view timeline, export CSV.
 */
import { TimeReportsPanel } from '../TimeReportsPanel'

export function ReportsPanel() {
  return <TimeReportsPanel variant="admin" />
}
